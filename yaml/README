On Kubernetes master, the following directories must exist

/etc/kubernetes/manifest
  - apiserver.yaml
  - podmaster.yaml
/srv/kubernetes/manifest
  - scheduler.yaml
  - controller-mgr.yaml

These directories and files are setup using start-k8sm.sh
